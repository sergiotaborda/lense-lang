
import lense.core.collections.Array;
import lense.core.lang.Maybe; // auto-import
import lense.core.lang.String; // auto-import
import lense.core.math.Integer; // auto-import
import helloworld.start.Matrix;

public class Dto {

	public constante : Integer;
  	public var name : String?;
  	public var age : Integer = 2;
	public var birthDay : Integer? ;  
  	public var address : String? {
  		get {
  			// direct access to backing field
  			return address;
  		}
  		set (value) {
  			// direct access to backing field
  			this.address = value;
  		}
  	}
  	public var matrix : Matrix<String>?;

	public var number : Natural  = 3 {
		get {
			// direct access to backing field
  			return number;
		}
	  	set (value) {
  			// direct access to backing field
  			this.number = value;
  		}
	};
}