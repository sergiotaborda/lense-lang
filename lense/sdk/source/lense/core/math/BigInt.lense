
import lense.core.math.Integer;
import lense.core.lang.String;

public native value class BigInt implements Integer {

	public native constructor parse(other : String);
	public native implicit constructor valueOf(other : Natural);
	
}