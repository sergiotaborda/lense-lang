
import lense.core.math.Integer;
import lense.core.lang.String;

public native value class BigNatural implements Natural {

	public native constructor parse(other : String);
	public native constructor zero();
	public native constructor one();
	public native constructor ten();

 	public native reduce(): Natural;
}